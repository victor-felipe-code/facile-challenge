generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model encrypted_name {
  id   Int    @id @unique @default(autoincrement())
  name String
}
